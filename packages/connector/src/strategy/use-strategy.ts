/**
 * Re-export strategy orchestrator and types from @scholaracle/contracts (single source of truth).
 */
export {
  useStrategy,
  computeFingerprint,
  type IExtractionStrategy,
  type ISelectorStep,
  type IStrategyAttempt,
  type IStrategyStore,
} from '@scholaracle/contracts';
