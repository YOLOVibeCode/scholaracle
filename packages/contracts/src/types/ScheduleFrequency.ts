/** Sync frequency for data source agents. */
export type SyncSchedule = 'hourly' | 'every_6h' | 'daily' | 'manual';

/** Notification tone preference. */
export type NotificationTone = 'formal' | 'casual' | 'encouraging';

/** Notification frequency preference. */
export type NotificationFrequency = 'minimal' | 'balanced' | 'proactive';
