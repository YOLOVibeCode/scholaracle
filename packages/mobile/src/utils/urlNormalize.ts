/**
 * Pure string-based URL helpers.
 * Canonical implementation lives in @scholaracle/studio-core (no URL API).
 */

export { normalizeUrl, extractHostname, isSameNormalizedUrl } from '@scholaracle/studio-core';
