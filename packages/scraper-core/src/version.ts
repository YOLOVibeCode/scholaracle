/**
 * EXTRACTOR_BUNDLE_HASH — content stamp for scraper-core extractors.
 * Updated by tests / release process when extractor sources change.
 */

export const SCRAPER_CORE_PACKAGE_VERSION = '0.1.0';

/**
 * Stable hash placeholder for v0.1; recipe/extractor tests assert presence.
 * A future build step can overwrite this with a real content hash.
 */
export const EXTRACTOR_BUNDLE_HASH = 'sha256:scraper-core-0.1.0-dev';
