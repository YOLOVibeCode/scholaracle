/**
 * Jest setup: extend expect with @testing-library/jest-dom matchers (e.g. toBeInTheDocument).
 * Loaded after test framework; only affects tests that run in jsdom.
 */
import '@testing-library/jest-dom';
